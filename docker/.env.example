# Copy this file to docker/.env and fill in your values

# Required — your Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Optional — override the directory mounted as /workspace inside the container
# Defaults to the repo root (.) if not set
# WORKSPACE_PATH=/Users/you/my-project
